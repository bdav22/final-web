@page "{id:int}"
@model TaskManager.Pages.DeleteModel
@{
    var contact = Model.Task;
}

<p>Are you sure to delete @($"{contact.Id} {contact.Description}")?</p>

<form asp-page="Delete">
    <button type="submit" class="text-danger h1">Delete</button>
</form>
